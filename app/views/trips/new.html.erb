<%= render "shared/header"%>

<div class="contents row">
  <div class="container">
    <h3>新しい旅行の予定を作成</h3>
    <%= render partial: "form", locals: { trip: @trip } %>
  </div>
</div>

<%= render "shared/footer" %>